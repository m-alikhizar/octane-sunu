language: node_js

node_js:
  - "9"

sudo: true
dist: trusty

branches:
  only:
  - master

cache:
  directories:
    - node_modules

addons:
  chrome: stable

before_script:
    - "sudo chown root /opt/google/chrome/chrome-sandbox"
    - "sudo chmod 4755 /opt/google/chrome/chrome-sandbox"

script: 
  - ng lint
  - ng test
  - ng build
#  - npm run e2e
